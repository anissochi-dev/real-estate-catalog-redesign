[build]
  publish = "dist"
  command = "bun run build"

[[edge_functions]]
  path = "/*"
  function = "bot-render"
  # force = true гарантирует что edge-функция перехватывает запросы раньше _redirects
  # Без этого _redirects имеет более высокий приоритет и боты получают index.html
  force = true
