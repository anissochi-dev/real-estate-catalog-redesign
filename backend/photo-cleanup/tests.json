{
  "tests": [
    {
      "name": "Без авторизации — последний лог",
      "method": "GET",
      "path": "/",
      "expectedStatus": 200,
      "expectedBody": {},

      "bodyMatcher": "partial"
    },
    {
      "name": "Dry run без токена — 401",
      "method": "GET",
      "path": "/?action=dry_run",
      "expectedStatus": 401,
      "expectedBody": {"error": "string"},
      "bodyMatcher": "partial"
    }
  ]
}